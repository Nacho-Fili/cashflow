# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @cashflow/cashflow`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @cashflow/cashflow`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @cashflow/cashflow && \
          adduser --system -G @cashflow/cashflow @cashflow/cashflow

COPY apps/cashflow/dist @cashflow/cashflow/
RUN chown -R @cashflow/cashflow:@cashflow/cashflow .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @cashflow/cashflow --omit=dev -f install

CMD [ "node", "@cashflow/cashflow" ]
